node_modules
dist
.vite
coverage
*.log
.env
.env.*
!.env.example
.DS_Store
Thumbs.db
temp_semanal/
*.backup.html
Control vehicular Semanal ROF-export.zip
Seguimiento de unidades en el taller.xlsx

# Fixtures de smoke test servidos por Vite (no se commiten: son binarios grandes)
public/
# Excepción: assets PWA deben viajar con el repo (favicon.svg referenciado en manifest)
!public/favicon.svg
_legacy/
.husky/_

# Claude Code / IDE locales (launch.json sí se commitea)
.claude/settings.local.json
.vscode/
.idea/
*.swp

# Build extra
dev-dist/
tsconfig.tsbuildinfo

# TypeScript build output (tsc -b emite .js junto a .ts; usamos tsc --noEmit ahora)
src/**/*.js
src/**/*.js.map
src/**/*.d.ts
tests/**/*.js
tests/**/*.js.map

# Playwright
playwright-report/
test-results/
